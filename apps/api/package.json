{
  "name": "@app/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c wrangler.toml",
    "build": "wrangler deploy -c wrangler.toml --env=\"\" --dry-run --outdir=dist",
    "check": "tsc -p tsconfig.json --noEmit",
    "deploy": "wrangler deploy -c wrangler.toml --env=\"\""
  },
  "dependencies": {
    "@app/infra": "workspace:*",
    "@app/shared-zod": "workspace:*",
    "@app/sync-protocol": "workspace:*",
    "@sentry/cloudflare": "10.68.0",
    "hono": "4.12.32",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260724.1",
    "typescript": "5.9.3",
    "wrangler": "4.114.0"
  }
}
