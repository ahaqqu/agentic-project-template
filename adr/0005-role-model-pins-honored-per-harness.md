# ADR-0005: Role-agent model pins are the single source of truth, honored per harness

## Status

Accepted (2026-08-29). Records the model-routing decision for the manager-orchestrated agentic workflow. Originated in the KajianQ fork (issue #96, PR #98, probed 2026-08-29/30) and ported to the template; builds on the template-sync ownership rules in `template-sync.json`.

## Context

The manager skill (`.agents/skills/manager/SKILL.md`) dispatches six role agents defined in `.zcode/agents/*.md`, each with a `model:` pin in its frontmatter. The repo runs two harnesses: **ZCode**, which parses those files natively and resolves pins through its override order, and **DSH (DeepSeek Harness)**, which parses none of them.

Verified against the installed DSH (live probes, 2026-08-29): `subagent` children always inherit the session model (no per-call model parameter, no `subagent_type`); the `workflow` tool's `agent(prompt, { provider, model })` override is the only model-facing per-agent route and its children are one-shot (no continuation); nested spawns work from both plain-subagent and workflow children, so the reviewer can dispatch its two sub-reviewers from either. An initial probe round showed `glm-5.3` failing on DSH while ids declared in the DSH provider's model list dispatched — root cause was a missing declaration, not an upstream gap: ollama.com serves `glm-5.3` (catalog-verified), and after declaring it in `~/.dsh/settings.yaml` (hot-reloaded) the pin routes. Every template pin now dispatches on DSH. Issue #96's earlier non-goal ("no changes to the pinned role models") predates this owner request to make DSH work for every ZCode-configured role.

**Known gap — ZCode-side pin resolution (recorded 2026-08-30, from PR #98's review).** On ZCode the pin's provider id must match a provider **key** in the client's provider config (`~/.zcode/v2/config.json`). This machine's ollama provider was keyed by its client-generated UUID, so *every* pinned role failed to spawn with "Model provider is not configured: ollama" — reproduced live for all six roles during the PR #98 review (which is how the review's thermo sub-reviewer passes reached the fallback `thermos-with-comments` already allows). The config was fixed by adding an `ollama`-keyed provider entry carrying the same baseURL/models (the client's spawn-time registry is cached from startup, so a mid-session fix needs a client restart to take effect — restart-verification pending). The gap motivated the ZCode-side preflight gate (`bun run zcode:preflight`, `scripts/zcode-pin-check.mjs`) mirroring `dsh:preflight`: it checks every role pin's provider key and model id against the client's provider config and exits non-zero with the exact config fix — the same doctrine, both harnesses. It reads the config file only (no writes: the entry carries credentials and the client owns its keying) and cannot verify the running client's startup cache.

## Decision

1. **`.zcode/agents/<role>.md` frontmatter `model:` is the single source of truth for role models on every harness.** Model identity is never duplicated into skills, prompts, or per-harness role variants. The *mechanism* of honoring a pin is per-harness; the *value* travels from one file.
2. **Every harness honors the pin.** ZCode does so natively (override order: user → project pin → template default; adapter: `.agents/skills/manager/ZCODE-ADAPTER.md`). DSH does so through its adapter (`.agents/skills/manager/DSH-ADAPTER.md`): at dispatch, read the pin, translate `ollama/<model>:cloud` → `{ provider: "ollama", model: "<model>" }`, then dispatch — plain `subagent` (continuable) when the pinned model equals the session model, otherwise a `workflow` `agent()` call (one-shot; a CI-red relay respawns a fresh agent carrying the failing logs instead of resuming).
3. **A pin that fails to resolve on a harness is a harness-config gap, fixed in the harness's own configuration — never rerouted to a different model.** On DSH the fix ships as a script, not wording: the preflight gate `bun run dsh:preflight` (`scripts/dsh-pin-check.mjs`) reads every role pin from `.zcode/agents/`, checks each model id against the provider's declared models (`~/.dsh/settings.yaml`) and the ollama.com catalog, and exits non-zero with the fix; `--fix` appends missing declarations — atomically, with a timestamped backup, only for ids the catalog serves (an unserved id is a pin/provider change, never a declaration). On ZCode, `bun run zcode:preflight` (`scripts/zcode-pin-check.mjs`) checks each pin's provider key and model id against the client's provider config (`~/.zcode/v2/config.json`) and exits non-zero with the config fix; it never writes the client config (credentials and keying are the client's own). Per-harness fallback models are rejected: they fork the pin truth, make the run silently weaker than the pin declares, and bury the deviation inside skills. The role files keep their template pins untouched (template-sync conflicts avoided). Skill and adapter files record no fallback tables — the *absence* of routing deviations is the invariant; the gates' exit codes are the check, and probe evidence and dates live in this ADR and the PR, not in skills.
4. **Free-tier compatibility holds:** all ids stay on the existing ollama provider — no new vendor and no paid service on the critical path (AGENTS.md universal guardrails).

## Consequences

**Update (2026-09-01, PR #130 — supersedes the ZCode preflight machinery, not the decision).** ZCode's agent editor now rewrites role files on save (quoted frontmatter values, `custom:<uuid>:<model>` pins, client-managed `thoughtLevel`), which the static machinery gate could not track — the gate hard-failed every ZCode-edited role. The machinery enforcement was removed entirely: `scripts/zcode-machinery-check.mjs` and `scripts/zcode-pin-check.mjs` (`bun run zcode:preflight`) are deleted, and `.zcode/` moved from `overwrite` to `merge` in `template-sync.json` (forks inherit template updates and may customize locally). Pins are client-managed: a non-resolving pin surfaces at dispatch time as "Model provider is not configured: <id>", the dispatch-time check that ZCode-ADAPTER.md now points to. The DSH preflight (`dsh:preflight`) is unchanged. The ollama-channel paragraph below retains its historical caching-channel rationale; the `builtin:`-channel pin enforcement it described no longer exists in CI.

**Update (2026-08-31, issue #125 — supersedes the ollama-channel specifics below, not the decision).** The committed pins moved to the caching provider channel `builtin:zai-start-plan/GLM-5.3-Flash` (one concrete pin per role; evidence: 225M cache-read tokens across ~3K requests on that channel vs zero on the ollama channel), and `.zcode/` became template-owned (`overwrite` in `template-sync.json`) — so "the role files keep their template pins untouched (template-sync conflicts avoided)" now reads: forks inherit the pins wholesale and re-pin via the ZCode user-scope override (`~/.zcode/agents/<role>.md`); DSH still has no override mechanism, so DSH runners must declare/serve the pinned id in `~/.dsh/settings.yaml` (re-run `bun run dsh:preflight` after any pin change) or the DSH pin truth must be re-adjudicated. The ZCode preflight changed shape: structural machinery violations (hook wiring, concrete `model:`/`thoughtLevel:` pins) exit non-zero and are also enforced by `bun run template-gate`; a pin that does not resolve in the local provider config is a visible drift *warning* (CI has no client config), not a gate failure.

- All six ZCode-configured roles run end-to-end on DSH with **their exact pinned models** — verified by probe on 2026-08-29 after the provider declaration; the DSH adapter documents no routing deviations. On ZCode, pin resolution is config-dependent (see the known-gap note in Context): after the `ollama`-keyed provider entry fix, `bun run zcode:preflight` verifies the pins against the client config; a ZCode run must pass that gate before the "every pinned role" claim is made.
- Model changes remain one-file edits: change a pin and re-probe the new id on each harness before the next manager run (`dsh:preflight` / `zcode:preflight`); a failure is fixed by declaring the id or the provider key in harness config, in the same change.
- Workflow-pinned dispatches are one-shot; the manager compensates by respawning fresh with logs, so continuation is never silently lost — it changes form per dispatch type.
- Skills carry no routing status, fallback tables, or support-status sections that can go stale; their only routing claim is the dispatch rule and the config-fix procedure.

## Revisit triggers

- A role pin changes in `.zcode/agents/`: re-probe the new id on both harnesses before the next manager run; if it fails to resolve, declare the id (DSH) or fix the provider key/model list (ZCode) rather than rerouting.
- A harness upgrade changes model-resolution behavior: re-run the probe suite (spawn, continuation, nested-from-workflow, per-pin routing) before trusting the loop.
- A third harness is adopted: give it an adapter file in the manager skill, its own pin-honoring rule, and its own preflight gate — the pin file does not move.
